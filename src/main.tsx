import React from 'react';
import ReactDOM from 'react-dom/client';
import InventoryComparer from './InventoryComparer';
import './App.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <InventoryComparer />
  </React.StrictMode>
);
